services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    env_file: .env
    command: ["redis-server", "/usr/local/etc/redis/redis.conf", "--requirepass", "${REDIS_PASSWORD}"]
    volumes:
      - ./infra/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "REDISCLI_AUTH=\"$${REDIS_PASSWORD}\" redis-cli ping | grep PONG"]
      interval: 10s
      timeout: 5s
      retries: 6
    networks: [agents]

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file: .env
    volumes:
      - pg-data:/var/lib/postgresql/data
      - ./infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 6
    networks: [agents]

  qdrant:
    image: qdrant/qdrant:latest
    restart: unless-stopped
    env_file: .env
    command: ["./qdrant", "--config-path", "/qdrant/config/config.yaml"]
    volumes:
      - qdrant-data:/qdrant/storage
      - ./infra/qdrant/config.yaml:/qdrant/config/config.yaml:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:6333/healthz"]
      interval: 10s
      timeout: 5s
      retries: 6
    networks: [agents]

  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - ollama-data:/root/.ollama
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:11434/api/version"]
      interval: 10s
      timeout: 5s
      retries: 6
    networks: [agents]

  lemonade:
    image: python:3.12-alpine
    restart: unless-stopped
    env_file: .env
    command:
      - sh
      - -lc
      - |
        python - <<'PY'
        from http.server import BaseHTTPRequestHandler, HTTPServer
        class Handler(BaseHTTPRequestHandler):
            def do_GET(self):
                if self.path == '/health':
                    self.send_response(200)
                    self.end_headers()
                    self.wfile.write(b'ok')
                else:
                    self.send_response(404)
                    self.end_headers()
            def log_message(self, format, *args):
                pass
        HTTPServer(('0.0.0.0', 8000), Handler).serve_forever()
        PY
    volumes:
      - lemonade-models:/models
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 6
    networks: [agents]

  orchestrator:
    build:
      context: .
      dockerfile: orchestrator/Dockerfile
    env_file: .env
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }
      qdrant: { condition: service_healthy }
      lemonade: { condition: service_healthy }
      ollama: { condition: service_healthy }
      home_automation: { condition: service_started }
      personal_assistant: { condition: service_started }
      system_health: { condition: service_started }
      storage_backup: { condition: service_started }
      knowledge_notes: { condition: service_started }
      household_ops: { condition: service_started }
      entertainment: { condition: service_started }
    networks: [agents]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  home_automation:
    build:
      context: .
      dockerfile: agents/home_automation/Dockerfile
    env_file: .env
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }
      lemonade: { condition: service_healthy }
      ollama: { condition: service_healthy }
    networks: [agents]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  personal_assistant:
    build:
      context: .
      dockerfile: agents/personal_assistant/Dockerfile
    env_file: .env
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }
      lemonade: { condition: service_healthy }
      ollama: { condition: service_healthy }
    networks: [agents]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  system_health:
    build:
      context: .
      dockerfile: agents/system_health/Dockerfile
    env_file: .env
    pid: "host"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }
      lemonade: { condition: service_healthy }
      ollama: { condition: service_healthy }
    networks: [agents]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  storage_backup:
    build:
      context: .
      dockerfile: agents/storage_backup/Dockerfile
    env_file: .env
    volumes:
      - /mnt:/mnt:ro
      - /home:/host-home:ro
      - ./infra/storage_backup/backup_targets.yaml:/etc/storage_backup/backup_targets.yaml:ro
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }
      lemonade: { condition: service_healthy }
      ollama: { condition: service_healthy }
    networks: [agents]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  knowledge_notes:
    build:
      context: .
      dockerfile: agents/knowledge_notes/Dockerfile
    env_file: .env
    volumes:
      - ./data/notes:/data/notes
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }
      lemonade: { condition: service_healthy }
      ollama: { condition: service_healthy }
    networks: [agents]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  household_ops:
    build:
      context: .
      dockerfile: agents/household_ops/Dockerfile
    env_file: .env
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }
      lemonade: { condition: service_healthy }
      ollama: { condition: service_healthy }
    networks: [agents]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  entertainment:
    build:
      context: .
      dockerfile: agents/entertainment/Dockerfile
    env_file: .env
    volumes:
      - ./data/media:/data/media
    depends_on:
      redis: { condition: service_healthy }
      postgres: { condition: service_healthy }
      lemonade: { condition: service_healthy }
      ollama: { condition: service_healthy }
    networks: [agents]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 5

networks:
  agents:
    driver: bridge

volumes:
  redis-data:
  pg-data:
  qdrant-data:
  ollama-data:
  lemonade-models:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./infra/lemonade/models
