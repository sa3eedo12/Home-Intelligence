# Redis cannot read env vars in redis.conf. Set auth via compose command:
# redis-server /usr/local/etc/redis/redis.conf --requirepass "$REDIS_PASSWORD"
appendonly yes
appendfsync everysec
maxmemory 1gb
maxmemory-policy allkeys-lru
